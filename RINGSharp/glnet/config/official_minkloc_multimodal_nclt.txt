[MODEL]
model = official_minkloc_multimodal_nclt
use_rgb = True
use_panorama = False
cam_id = 0,1,2,3,4

use_xyz = False
use_bev = False
use_range_image = False
normalize = True

# Path to a clone of https://github.com/jac99/MinkLocMultimodal.
# You can also set MINKLOC_MULTIMODAL_ROOT instead of editing this value.
official_minkloc_root = /root/autodl-fs/ringsharp/MinkLocMultimodal

# NCLT Cam1 is the first image returned by NCLTPointCloudWithImageLoader.
# This is used as the forward monocular image input for the zero-shot baseline.
minkloc_image_cam = 0

# Official MinkLoc++ consumes 4096 points normalized to approximately [-1, 1]
# and sparse-quantized with 0.01 voxel size.
minkloc_num_points = 4096
minkloc_quantization_size = 0.01
minkloc_normalize_pointcloud = True
minkloc_pointcloud_sample_seed = 17

output_dim = 256
feature_dim = 256

loss = BatchHardTripletMarginLoss
margin = 0.2

Height = 224
Width = 384
